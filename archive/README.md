# archive

Old configs kept for reference but **no longer part of the active setup** —
nothing here is installed or symlinked by `install.sh`.

| Archived | Replaced by |
|----------|-------------|
| `yabai/`, `config/yabai/` | AeroSpace tiling WM — `aerospace/` |
| `skhd/`, `config/skhd/` | AeroSpace key bindings |
| `alacritty/`, `config/alacritty/` | Ghostty — `ghostty/` |
| `config/fish/` | zsh — `.zshrc` |

Safe to delete entirely if you never want to look back.
